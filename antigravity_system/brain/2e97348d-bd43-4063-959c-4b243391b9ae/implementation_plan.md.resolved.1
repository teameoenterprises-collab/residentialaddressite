# Gravity Claw: Telegram & MCP Integration Finalization

Finalize the setup of the Gravity Claw agent ("Max") and ensure it can autonomously manage the website via Telegram using MCP tools.

## User Review Required

> [!IMPORTANT]
> The bot requires `TELEGRAM_BOT_TOKEN`, `AUTHORIZED_TELEGRAM_USER_ID`, and `GEMINI_API_KEY` to be set in a `.env` file within the `agents/gravity-claw` directory. 

## Proposed Changes

### Gravity Claw Agent

#### [MODIFY] [.env](file:///Users/tahmidnur/.gemini/antigravity/scratch/theresidentialaddress/agents/gravity-claw/.env)
- Ensure all required keys are present:
    - `TELEGRAM_BOT_TOKEN`
    - `AUTHORIZED_TELEGRAM_USER_ID`
    - `GEMINI_API_KEY`
    - `HQ_GROUP_ID` (for Chief of Staff mode)

#### [VERIFY] [loop.ts](file:///Users/tahmidnur/.gemini/antigravity/scratch/theresidentialaddress/agents/gravity-claw/src/agent/loop.ts)
- Confirm the `gemini-2.5-flash` model is correctly handling the `filesystem__` tools.
- Verify the routing logic for different chat IDs (HQ vs Personal).

## Verification Plan

### Automated Tests
- I will run `npm run dev` in the `agents/gravity-claw` directory to ensure the bot starts and connects to the MCP servers without error.
- I will check the console logs for "✅ Successfully connected to MCP: filesystem".

### Manual Verification
- The user should send a test message to the bot on Telegram (e.g., "What time is it?" or "List the files in the project") to verify the agent loop and tool execution.
